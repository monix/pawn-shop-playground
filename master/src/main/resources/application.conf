{
  http-server {
    host: "localhost"
    port: 8080
    end-point: "http://"${server.host}":"${server.port}
  }

  grpc-server {
    host: "localhost"
    port: 9081
    end-point: "http://"${server.host}":"${server.port}
  }
}